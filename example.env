# ============================================
# Mini-Math Development Environment Variables
# ============================================
# Copy this file to .env and update with your values
# cp env.example .env

# ============================================
# Server Configuration
# ============================================

# Server port (optional, used if DOMAIN not set)
PORT=3003

# Server domain - can be set as full string or built from DOMAIN_HOST/DOMAIN_PORT
# Option 1: Full domain string
DOMAIN=localhost:3003

# Option 2: Build from host and port (used if DOMAIN not set)
# DOMAIN_HOST=localhost
# DOMAIN_PORT=3003

# SIWE (Sign-In With Ethereum) domain (defaults to DOMAIN if not set)
SIWE_DOMAIN=localhost:3000

# CORS Configuration
# Option 1: Single origin (CORS_ORIGIN - preferred if single origin)
CORS_ORIGIN=http://localhost:3000

# Option 2: Multiple origins (ALLOWED_ORIGINS - comma-separated, used if CORS_ORIGIN not set)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3003

# Trust proxy setting for reverse proxies (default: false in dev, true in production)
# Set to 'true' if running behind a reverse proxy 
TRUST_PROXY=false

# ============================================
# Platform & Authentication
# ============================================

# Platform owner wallet address (default: 0x89c27f76eef3e09d798fb06a66dd461d7d21f111)
# This is the initial admin/owner address for the platform
INIT_PLATFORM_OWNER=0x89c27f76eef3e09d798fb06a66dd461d7d21f111

# Session secret (use a long random string in production)
SESSION_SECRET=super-long-session-secret

# Secure cookies (set to true in production with HTTPS)
SECURE=false

# ============================================
# User Management (Development Features)
# ============================================

# ============================================
# Logging & Debugging
# ============================================

# Enable verbose session logging (default: true)
# Set to 'false' to reduce session-related log noise
VERBOSE_SESSION_LOGGING=true

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# External Services
# ============================================

# Etherscan API key (required for some features)
# Get your API key from: https://etherscan.io/apis
ETHERSCAN_APIKEY=your_etherscan_api_key_here


# ============================================
# Notes
# ============================================
# - All values have sensible defaults for local development
# - In production, ensure TRUST_PROXY=true if behind a reverse proxy
# - Use CORS_ORIGIN for single origin, ALLOWED_ORIGINS for multiple
# - SECURE should be true in production with HTTPS
# - SESSION_SECRET should be a long random string in production
# - ETHERSCAN_APIKEY is required for blockchain-related features

